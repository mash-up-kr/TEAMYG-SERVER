spring:
  datasource:
    url: jdbc:mysql://localhost:3306/parfait?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&rewriteBatchedStatements=true
    username: root
    password: rootpass
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    org.hibernate.SQL: DEBUG

management:
  endpoint:
    health:
      show-details: always

jwt:
  secret-key: local-only-dummy-jwt-secret-key-please-change-32b

kakao:
  app-key: local-only-dummy-kakao-app-key
